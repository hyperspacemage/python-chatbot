# Easy to use chatbot using Python and Ollama.

### Usage
You need to create a Python Virtual Environment.
```bash
python -m venv venv
```
Then install ```ollama``` and ```rich```
```bash
pip install ollama rich
```
Run with:
```bash
python chatbot.py
```
